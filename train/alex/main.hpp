#pragma once

#include <cstdio>
#include <string>
#include <vector>
#include <algorithm>

#include "../../util.hpp"

std::vector<std::string> solve1(const std::vector< std::vector<bool> > &data);
std::vector<std::string> solve2(const std::vector< std::vector<bool> > &data);

